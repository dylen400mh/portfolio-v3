export const metadata = {
  title: "Post title",
  date: "2026-01-01",
  description: "One-line summary shown on the blog index and in <meta>/OG tags.",
};

Write the post body in Markdown / MDX below the export. Headings, lists, links,
**bold**, tables, and blockquotes all work.

## A section heading

Fenced code blocks are highlighted by Shiki. Add an optional title bar with the
`title` attribute:

```ts title="example.ts"
export const x = 42;
```

> This file is a reference template. Because its name starts with `_`, it is
> excluded from the blog index and never gets a route — copy it to
> `content/<your-slug>.mdx` to publish a real post.
